pipeline {
  agent any
  stages {
    stage("pytest build") {
      steps {
        bat """
          docker build -t pytest-project .
        """
      }
    }
    stage('Unit Test') {
      steps {
        echo "test"
      }
    }
  }
}