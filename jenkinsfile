pipeline {
  agent any
  stages {
    stage("pytest build") {
      steps {
        bat """
          docker build -t pytest-project .
        """
        junit '**/reports/junit/*.xml'
      }
    }
    stage('Unit Test') {
      steps {
        echo "test"
      }
    }
  }
}