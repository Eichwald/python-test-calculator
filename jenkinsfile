pipeline {
  agent any
  stages {
    stage('exe Test') {
      steps {
        bat "ADD_CLI.exe -- /X:5 /Y:10"
      }
    }
    stage('pytest') {
      steps {
        bat "pytest"
      }
    }
  }
}