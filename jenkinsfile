pipeline {
  agent any
  stages {
    stage("pytest build") {
      steps {
        bat "pytest --junitxml=reports/result.xml"
      }
    }
    stage('Unit Test') {
      steps {
        bat "cd C:\\Program Files\\Google\\Chrome\\Application"
        bat "chrome.exe"
      }
    }
  }
}