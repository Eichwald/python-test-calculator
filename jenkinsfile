pipeline {
  agent any
  stages {
    stage('exe Test') {
      steps {
        bat "start cmd /c ADD_CLI.exe -- /X:5 /Y:10"
      }
    }
    stage('pytest') {
      steps {
        bat "pytest --junitxml=testreport.xml"
        junit "testreport.xml"
      }
    }
  }
}