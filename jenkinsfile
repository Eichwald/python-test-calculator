pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        sh """
          docker build -t pytest-project .
        """
      }
    }
    stage("run") {
      steps {
        sh """
          docker run --rm pytest-project
        """
      }
    }
  }
}