pipeline {
  agent any
  stages {
    stage("pytest build") {
      steps {
        bat "pytest --junitxml=reports/result.xml"
      }
    }
    stage('Unit Test') {
      steps {
        echo "test"
      }
    }
  }
}