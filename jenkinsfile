pipeline {
  agent any
  stages {
    stage("pytest build") {
      steps {
        bat "pytest --junitxml=reports/result.xml"
      }
    }
    stage('Unit Test') {
      steps {
        bat "start ADD_CLI.exe -- /X:5 /Y:8"
      }
    }
  }
}